{
  "build": {
    "env": {
      "APP_ENV": "production",
      "APP_URL": "https://web-desa-medalsari-production.up.railway.app",
      "ASSET_URL": "https://web-desa-medalsari-production.up.railway.app"
    },
    "command": "composer install && npm install && npm run build && php artisan key:generate && php artisan migrate --force && php artisan config:cache"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=8080"
  }
}
